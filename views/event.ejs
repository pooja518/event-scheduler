<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css" rel="stylesheet">
    <meta name="csrf-token" content="<%= csrfToken %>"/>
    <link href="./css/styles.css" rel="stylesheet" />
    <title><%= title %></title>
</head>

<body class="bg-purple-100 min-h-screen flex justify-center items-center">
    <div class="bg-white p-8 rounded shadow-md text-center">
        <h6 class="text-2xl font-bold text-purple-600 mb-6">Create a New Event</h6>
        <form action="/create-event" method="POST" class="flex flex-col items-start w-full max-w-md"> <!-- Use flex-col and items-start for column layout, and set a max-width -->
            <input name="_csrf" type="hidden" value="<%= csrfToken %>">
            <div class="mb-4 flex flex-col sm:flex-row"> <!-- Flex container for each label-input pair with column layout on small screens and row layout on larger screens -->
                <div class="flex-none w-36 mr-4 mb-2 sm:mb-0"> <!-- Fixed width for labels on larger screens -->
                    <label for="eventName" class="text-black-200 block">Event Name:</label> <!-- Label for Event Name -->
                </div>
                <div class="flex-1"> <!-- Flexible width for input fields -->
                    <input type="text" name="eventName" class="border border-gray-300 rounded w-full p-2 text-gray-900 text-sm"> <!-- Input for Event Name -->
                </div>
            </div>
            
            <div class="mb-4 flex flex-col sm:flex-row"> <!-- Flex container for each label-input pair with column layout on small screens and row layout on larger screens -->
                <div class="flex-none w-36 mr-4 mb-2 sm:mb-0"> <!-- Fixed width for labels on larger screens -->
                    <label for="eventDescription" class="text-black-200 block">Event Description:</label> <!-- Label for Event Description -->
                </div>
                <div class="flex-1"> <!-- Flexible width for input fields -->
                    <textarea name="eventDescription" class="border border-gray-300 rounded w-full p-2 text-gray-900 text-sm"></textarea> <!-- Input for Event Description -->
                </div>
            </div>
            
            <!-- Repeat the above pattern for other form fields (eventDate, participants, eventVenue) -->
            
            <div class="mb-4 flex flex-col sm:flex-row"> <!-- Flex container for each label-input pair with column layout on small screens and row layout on larger screens -->
                <div class="flex-none w-36 mr-4 mb-2 sm:mb-0"> <!-- Fixed width for labels on larger screens -->
                    <label for="eventDate" class="text-black-200 block">Event Date:</label> <!-- Label for Event Date -->
                </div>
                <div class="flex-1"> <!-- Flexible width for input fields -->
                    <input type="date" name="eventDate" class="border border-gray-300 rounded w-full p-2 text-gray-900 text-sm"> <!-- Input for Event Date -->
                </div>
            </div>
            
            <!-- ... (similar structure for participants and eventVenue) ... -->
                   
            <div class="mb-4 flex flex-col sm:flex-row"> <!-- Flex container for each label-input pair with column layout on small screens and row layout on larger screens -->
                <div class="flex-none w-36 mr-4 mb-2 sm:mb-0"> <!-- Fixed width for labels on larger screens -->
                    <label for="participants" class="text-black-200 block">Capacity:</label> <!-- Label for Number of Participants -->
                </div>
                <div class="flex-1"> <!-- Flexible width for input fields -->
                    <input type="number" name="participants" class="border border-gray-300 rounded w-full p-2 text-gray-900 text-sm"> <!-- Input for Number of Participants -->
                </div>
            </div>
            
            <div class="mb-4 flex flex-col sm:flex-row"> <!-- Flex container for each label-input pair with column layout on small screens and row layout on larger screens -->
                <div class="flex-none w-36 mr-4 mb-2 sm:mb-0"> <!-- Fixed width for labels on larger screens -->
                    <label for="eventVenue" class="text-black-200 block">Event Venue:</label> <!-- Label for Event Venue -->
                </div>
                <div class="flex-1"> <!-- Flexible width for input fields -->
                    <input type="text" name="eventVenue" class="border border-gray-300 rounded w-full p-2 text-gray-900 text-sm"> <!-- Input for Event Venue -->
                </div>
            </div>
            <div class="py-3">
                <button type="submit"
                    class="bg-purple-500 text-white rounded px-5 py-2 font-medium hover:bg-purple-700 focus:outline-none focus:ring focus:border-green-300">
                    Create Event
                </button>
            </div>
        </form>
    </div>
</body>

</html>












