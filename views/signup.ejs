<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css" rel="stylesheet">
    <meta name="csrf-token" content="<%= csrfToken %>"/>
    <link href="./css/styles.css" rel="stylesheet" />
    <title><%= title %></title>
</head>

<body class="bg-purple-50 min-h-screen flex justify-center items-center">
    <div class="bg-white p-8 rounded shadow-md text-center">
        <h6 class="text-xl font-bold text-purple-600 mb-6">Signup as a new user</h6>
        <form action="/users" method="POST" class="flex flex-col items-start"> <!-- Use flex-col and items-start for column layout -->
            <input name="_csrf" type="hidden" value="<%= csrfToken %>">
            <div class="flex mb-4 items-center"> <!-- Flex container for labels -->
                <label for="firstName" class="text-black-200 w-24 whitespace-no-wrap flex-shrink-0">First Name:</label> <!-- Set a fixed width for labels -->
                <input type="text" name="firstName" class="border border-gray-300 rounded w-full p-2 text-gray-900 text-sm">
            </div>
            <div class="flex mb-4 items-center"> <!-- Flex container for labels -->
                <label for="lastName" class="text-black-200 w-24 whitespace-no-wrap flex-shrink-0">Last Name:</label> <!-- Set a fixed width for labels -->
                <input type="text" name="lastName" class="border border-gray-300 rounded w-full p-2 text-gray-900 text-sm">
            </div>
            <div class="flex mb-4 items-center"> <!-- Flex container for labels -->
                <label for="email" class="text-black-200 w-24 whitespace-no-wrap flex-shrink-0">Email:</label> <!-- Set a fixed width for labels -->
                <input type="email" name="email" class="border border-gray-300 rounded w-full p-2 text-gray-900 text-sm">
            </div>
            <div class="flex mb-4 items-center"> <!-- Flex container for labels -->
                <label for="password" class="text-black-200 w-24 whitespace-no-wrap flex-shrink-0">Password:</label> <!-- Set a fixed width for labels -->
                <input type="password" name="password" class="border border-gray-300 rounded w-full p-2 text-gray-900 text-sm">
            </div>
            <div class="py-3">
                <button type="submit"
                    class="bg-green-500 text-white rounded px-5 py-2 font-medium hover:bg-green-700 focus:outline-none focus:ring focus:border-green-300">
                    Sign Up
                </button>
            </div>
        </form>
        <% if(messages && messages.error && messages.error.length > 0)  { %>
        <% for(var i=0; i<messages.error.length; i++) { %>
        <li class="text-red-500"><%= messages.error[i] %></li>
        <% } %>
        <% } %>
    </div>
</body>

</html>
